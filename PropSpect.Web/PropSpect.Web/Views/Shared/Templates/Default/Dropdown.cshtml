@model EditControl
@{
    ViewBag.Title = "Textbox";

    MvcHtmlString binding = new MvcHtmlString("");

    if (Model.IsAsync)
    {
        binding = new MvcHtmlString(string.Format("data-bind=\"options: {0} , optionsText:\'Text\',optionsValue:\'Value\', value: EditItem().{1}\"", Model.Source, Model.VariableName));
    }
}

@*<div class="form-group row">
        <label for="@Model.Label" class="col-sm-4 control-label">@Model.Label</label>
        <div class="col-sm-8">
            <input  type="text" class="form-control" name="@Model.Label" id="@Model.HtmlID"placeholder="@Model.Label" value="@Model.Value">
            @Model.Validation
        </div>
    </div>*@


<div class="input-field col s12  browser-default">
    @*<select placeholder="@Model.Label" id="@Model.HtmlID" class="validate" @binding></select>
    <label for="@Model.HtmlID">@Model.Label</label>
    @Model.Validation*@

    <select @binding></select>
    <label>@Model.Label</label>
</div>

@*<div class="input-field col s12">
    <select class="">
        <option value="" disabled selected>Choose your option</option>
        <option value="1">Option 1</option>
        <option value="2">Option 2</option>
        <option value="3">Option 3</option>
    </select>
    <label>Materialize Select</label>
</div>*@